<div class="container narrow" style="margin-top: 100px; font-size: 11px; ">
	<ul>
		<% @events.each do |event| %>
			<li>
				<%= event.story %> on <%= event.created_at.strftime("%b %-d") %>
			</li>
		<% end %>
	</ul>
</div>