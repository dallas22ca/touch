<% if @fship.errors.any? %>
	alert("<%= @fship.errors.full_messages.join(", ").html_safe %>")
<% else %>
	$("<%= j render @fship %>").appendTo(".channel_<%= @channel.id %>_channelships")
	$("#new_channelship")[0].reset()
	Lightbox.close()
<% end %>