<% if @task.errors.any? %>
	$("#<%= dom_id @task %>").replaceWith("<%= j render @task.reload %>")
<% end %>

<% if @channelship.permits? :tasks, :write %>
	<% if params[:task] && params[:task][:complete] %>
		<% if @task.complete %>
			$("#<%= dom_id @task %>").remove()
			$("<%= j render @task %>").prependTo(".channel_<%= @channel.id %>_completed_tasks")
		<% else %>
			$("#<%= dom_id @task %>").remove()
			$("<%= j render @task %>").appendTo(".channel_<%= @channel.id %>_tasks")
			$("#tasks").trigger("sortupdate")
		<% end %>
	<% end %>
<% end %>

<% if @channel.tasks.complete.any? %>
	$(".show_completed_tasks").show()
<% else %>
	$(".show_completed_tasks").hide()
<% end %>

Tasks.makeEditable()