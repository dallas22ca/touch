<% if @task.errors.any? %>
	$("#<%= dom_id @task %>").replaceWith("<%= j render @task.reload %>")
<% end %>

<% if @folder.tasks.complete.any? %>
	$(".show_completed_tasks").show()
<% else %>
	$(".show_completed_tasks").hide()
<% end %>